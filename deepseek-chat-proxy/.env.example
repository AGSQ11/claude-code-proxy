# DeepSeek Chat Proxy Configuration

# Required: Your DeepSeek auth token from browser localStorage
# Get this from: chat.deepseek.com > DevTools (F12) > Application > Local Storage > userToken
DEEPSEEK_AUTH_TOKEN="your_user_token_here"

# Optional: API key for client authentication
# If not set, any client can use the proxy
API_KEY="sk-deepseek-proxy"

# Optional: Server port
PORT="8090"
